<% include partials/header %>

<%= data["Search"].forEach(function(movie){ %>
 <li><%= movie["Title]%></li>



<ul>
   <% data["Search"].forEach(function(movie) { %>-->
       <li>
           <strong>
                <%= movie["Title"] %>
          </strong> 
              - <%=movie["Year"]%>
      </li>
    <% }) %>
</ul>

<% include partials/footer %>